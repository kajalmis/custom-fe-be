name: Backend CI/CD

on:
  push:
    

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Build Docker image
        run: |
          cd backend
          sudo docker build -t backend:latest .
          sudo docker images

  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Deploy to EC2
        uses: appleboy/ssh-action@master
        with:
          host: 3.84.61.52    
          username: ubuntu
          key: ${{ secrets.EC2_PRIVATE_KEY }}
          script: |
            sudo docker stop backend-container || true
            sudo docker rm backend-container || true
            sudo docker pull your-image-name:latest
            sudo docker run -d --name backend-container -p 5000:5000 backend:latest
